# 独立游戏资产与配置自动构建器 - Python依赖
# 核心依赖
pydantic>=2.5.0          # 数据验证和设置管理
requests>=2.31.0         # HTTP客户端（调用DeepSeek API）
python-dotenv>=1.0.0     # 环境变量管理
colorama>=0.4.6          # 跨平台彩色终端输出
click>=8.1.0             # 命令行界面创建

# 开发依赖（可选）
black>=23.0.0            # 代码格式化
pytest>=7.4.0            # 测试框架
pytest-cov>=4.1.0        # 测试覆盖率
mypy>=1.7.0              # 静态类型检查

# 可选：JSON处理增强
ujson>=5.8.0             # 更快的JSON解析（可选）
